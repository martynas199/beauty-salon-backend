import { Router } from "express"; import Appointment from "../models/Appointment.js"; const r=Router(); r.post("/stripe", async (req,res)=>{ const { appointmentId } = req.body || {}; if(appointmentId){ await Appointment.findByIdAndUpdate(appointmentId, { status:"confirmed", "payment.status":"succeeded", "payment.provider":"stripe" }); } res.json({ received:true }); }); export default r;
